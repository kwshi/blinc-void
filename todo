lxappearance

steam (font issue???), symlink

link/mount buildah containers

flatpak software: zoom, discord, etc. 
also zoom flatpak can't SSO, because zoom sucks
xbps-src actually, because flatpak is wak
pay attention to symlinks

game symlinks, e.g. klei dst

spotifyd, playerctl

default apps (browser), xdg-mime and xdg-open
specifically: use firefox, not chrome

pulse remember settings

polybar temperature warn icon
other polybar stuff
wm-restack=bspwm

chmod +x bspwmrc

powerline

jupyter notebook, misc. kernels

post-install hooks, managing gummiboot entries

sxhkd: keybinds for shutdown, fullscreen, float

xournal configs

ssd trim cron

compositor (picom), hsetroot background

/opt permissions

irc: irssi, weechat?

more desktops (4 or 5?)

github cli stuff, paste bins, auth session (symlink? or built-in?)

webcam app: cheese, fswebcam

bluetooth??

xsel/xclip

unicode cli

git: config pull, url git@github.com

factorrc

macos fonts
